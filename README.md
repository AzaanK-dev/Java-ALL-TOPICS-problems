## 📘 **ALL TOPICS JAVA PRACTICE PROBLEMS**

### **1. ABSTRACTION (ABSTRACT CLASSES)**
- Abstract Animal → Horse Implementation (runtime polymorphism)
- Library System → Abstract Library with Book, Magazine, EBook subclasses

### **2. CLASSES AND OBJECTS**
- Fraction Multiplier
- Items Stock Manager → purchase items, restock items, check stock
- Nested Classes → University > Depart (Inner) > Library (Static Nested)

### **3. COLLECTIONS**
- Student Sorting (marks desc, roll asc)
- Word Frequency Counter (HashMap)
- BST using Array of Node Objects (insert + root-to-node path)

### **4. CONDITIONAL LOGIC**
- Sales Management System → using 2D Array
- Percentage-based grading using Result class
- Performance-based salary increment (range-based)

### **5. CONSTRUCTORS**
- Multilevel Constructor Chain (A → B → C)
- Constructor Chaining (Branch → Company → Depart(inner) extends Branch)

### **6. ENUMS**
- Qualification Enum → salary switch-case
- Weekdays Enum → weekend check

### **7. FILE HANDLING / NETWORKING**
- Webpage Download + Auto-open in browser
- TCP Client–Server Chat (bidirectional)
- Reverse file lines
- Encrypt/Decrypt file (ASCII shift)
- Count spaces, tabs, newlines
- Recursive folder size calculator

### **8. GENERICS**
- Generic method to reverse any array (Integer/String/Double)

### **9. INHERITANCE & POLYMORPHISM**
- Shapes → Circle, Square (override area + display())
- Person → Employee → Faculty/Staff/Student (toString overriding)
- Vehicle → Car/Bike (constructor chaining + discount logic)

### **10. INTERFACES**
- Animal Interface → Horse Implementation

### **11. MULTITHREADING**
- Thread vs Runnable
- Shared Counter with 3 threads (inc/dec/reset using synchronized)
- Thread priorities (1, 5, 10)

### **12. RECURSION**
- Factorial (recursive) + call count
- Connected 1’s region counter (8-directional)
- Folder size calculation recursively → includes nested files & folders

### **13. STATIC BLOCK**
- Static vs Instance Blocks execution
- Static counter + Constructor Overloading
